import { AttachToDirective } from './attach-to.directive';

describe('AttachToDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachToDirective();
    expect(directive).toBeTruthy();
  });
});
